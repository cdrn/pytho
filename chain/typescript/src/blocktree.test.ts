import { assert } from 'console'
import BlockTree from './blocktree'

describe('Test the blocktree functionality', () => {
  test('the tests run', () => {
    expect(true)
  })
  test('Instantiating a blockchain makes a genesis block', () => {
    const blocktree = new BlockTree
    console.log(blocktree)
  })
  test('', () => {

  })
})